apiVersion: v1
kind: Service
metadata:
  name: ventas-backend
  namespace: innovatech-chile
spec:
  selector:
    app: ventas-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
